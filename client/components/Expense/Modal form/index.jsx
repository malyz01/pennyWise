import React, { Component } from 'react'
import { connect } from 'react-redux'
import { Modal } from 'semantic-ui-react'

export class ModalExpenseForm extends Component {
  render () {
    return (
      <Modal>

      </Modal>
    )
  }
}

export default connect()(ModalExpenseForm)
